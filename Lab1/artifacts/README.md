# Lab1 Artifacts

This directory is used for generated keys, encrypted files, signatures, decrypted files, and debug reports during CLI demonstrations.

Generated files are ignored by Git. Keep only this README and `.gitkeep` in the repository.

